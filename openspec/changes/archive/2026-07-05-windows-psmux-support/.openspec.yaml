schema: spec-driven
created: 2026-07-05
archived: 2026-07-21
status: experimental
reason: >
  Windows 支持的底层代码（process_info.rs / pty_io.rs 的 cfg(windows) 分支、detect_agent_kind
  扩展、control_mode.rs、Cargo.toml 的 sysinfo/windows-sys 依赖、install.ps1、install.js 平台
  映射、release workflow Windows 矩阵）均已实现并通过编译。

  但 Phase 0 兼容性 spike（tasks 1.1–1.7，要求在真实 Windows / windows-latest 上验证 psmux 多
  session、@user_option 展开、pane_pid ConPTY 语义、控制模式、pane_current_path 等关键场景）未
  完成，design.md 的 Open Questions 1–3 仍未被真实验证回答；另有多项收尾 task 未勾（5.4/5.5/
  6.3/6.6/7.2/7.5）。

  故以"实验性"状态归档：代码就绪但未经真实 Windows 端到端验证，docs/windows-support.md 已加
  实验性标注。待真实 Windows 验证通过后，可补一次 follow-up change 关闭遗留项并移除实验性标注。
