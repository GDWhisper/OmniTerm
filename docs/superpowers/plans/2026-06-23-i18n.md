# i18n 国际化实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 OmniTerm 前端添加中英双语 i18n 支持，使用 react-i18next，语言自动检测 + Settings 手动切换。

**Architecture:** 使用 react-i18next 的 `useTranslation()` hook 替换所有硬编码字符串。翻译文件按组件前缀组织为扁平 key。语言检测由 `i18next-browser-languagedetector` 插件自动处理（localStorage → navigator → en 回退）。语言状态完全由 i18next 管理，不入 Zustand。

**Tech Stack:** i18next, react-i18next, i18next-browser-languagedetector

## Global Constraints

- 回退语言为 `'en'`
- localStorage key 为 `'omniterm_locale'`
- 翻译 key 格式：`组件前缀.语义名`（如 `sidebar.createWorkspace`）
- 插值语法：`{{variable}}`
- 不翻译：`"px"`、`"项"`、产品名、版本号、License、后端 API 错误消息
- 语言按钮文案写死不翻译（中文按钮显示"中文"，英文按钮显示"English"）

---

### Task 1: 安装依赖

**Files:**
- Modify: `frontend/package.json`

- [ ] **Step 1: 安装 i18next 相关包**

```bash
cd /home/pax/coding/OmniTerm-dev/frontend
pnpm add i18next react-i18next i18next-browser-languagedetector
```

- [ ] **Step 2: 提交**

```bash
cd /home/pax/coding/OmniTerm-dev
git add frontend/package.json frontend/pnpm-lock.yaml
git commit -m "chore(deps): 添加 i18next、react-i18next、i18next-browser-languagedetector"
```

---

### Task 2: 创建 i18n 配置与翻译文件

**Files:**
- Create: `frontend/src/i18n.ts`
- Create: `frontend/src/locales/zh/translation.json`
- Create: `frontend/src/locales/en/translation.json`
- Modify: `frontend/src/main.tsx`

- [ ] **Step 1: 创建中文翻译文件**

Create `frontend/src/locales/zh/translation.json`:

```json
{
  "sidebar.expand": "展开侧边栏",
  "sidebar.collapse": "收起侧边栏",
  "sidebar.workspaces": "工作区",
  "sidebar.sessions": "会话",
  "sidebar.createWorkspace": "创建工作区",
  "sidebar.workspaceName": "名称",
  "sidebar.rootPath": "根路径",
  "sidebar.cancel": "取消",
  "sidebar.create": "创建",
  "sidebar.creating": "创建中...",
  "sidebar.createSession": "创建会话",
  "sidebar.sessionName": "会话名称",
  "sidebar.optional": "(可选)",
  "sidebar.deleteWorkspace": "删除工作区",
  "sidebar.deleteSession": "删除会话",
  "sidebar.confirmDeleteWorkspace": "确定要删除工作区「{{name}}」吗？该操作不可恢复。",
  "sidebar.confirmDeleteSession": "确定要删除会话「{{name}}」吗？对应的 tmux 会话也将被终止。",
  "sidebar.delete": "删除",
  "sidebar.workspaceCreated": "工作区「{{name}}」创建成功",
  "sidebar.sessionCreated": "会话「{{name}}」创建成功",
  "sidebar.workspaceDeleted": "工作区「{{name}}」已删除",
  "sidebar.sessionDeleted": "会话「{{name}}」已删除",
  "sidebar.unnamed": "未命名",
  "sidebar.connected": "已连接",
  "sidebar.disconnected": "未连接",
  "sidebar.noWorkspaces": "暂无工作区，点击 + 创建",
  "sidebar.noSessions": "暂无会话",
  "settings.title": "设置",
  "settings.theme": "主题",
  "settings.light": "亮色",
  "settings.dark": "暗色",
  "settings.system": "跟随系统",
  "settings.fontSize": "终端字体大小",
  "settings.language": "语言",
  "settings.about": "关于",
  "terminal.noSession": "选择或创建一个会话",
  "terminal.hint": "使用左侧边栏开始",
  "terminal.status.connected": "已连接",
  "terminal.status.disconnected": "连接断开",
  "terminal.status.connectionError": "连接错误",
  "terminal.status.attached": "已附加到 {{session}}",
  "terminal.status.error": "错误: {{msg}}",
  "terminal.status.exited": "进程退出: {{code}}",
  "fm.loadFailed": "加载文件列表失败",
  "fm.uploadComplete": "上传完成",
  "fm.uploadFailed": "上传失败",
  "fm.uploadFileFailed": "上传 {{name}} 失败: {{msg}}",
  "fm.renameSuccess": "重命名成功",
  "fm.renameFailed": "重命名失败",
  "fm.confirmDelete": "确定删除 {{count}} 个项目？",
  "fm.deleted": "已删除 {{count}} 个项目",
  "fm.deleteFailed": "删除失败",
  "fm.searchFailed": "搜索失败",
  "fm.selectSessionFirst": "请先在侧栏选择一个终端会话",
  "fm.loading": "加载中...",
  "fm.emptyDir": "此目录为空",
  "fm.dragHint": "拖放文件到此处上传",
  "fm.searchPlaceholder": "搜索文件名...",
  "fm.backToParent": "返回上级",
  "fm.search": "搜索文件",
  "fm.refresh": "刷新",
  "fm.upload": "上传文件",
  "fm.collapse": "收起文件管理器",
  "fm.expand": "展开文件管理器",
  "fm.rename": "重命名",
  "fm.delete": "删除",
  "fm.items": "项",
  "fm.backToTerminalDir": "回到终端当前目录",
  "fm.outOfWorkspace": "当前目录超出 workspace 边界",
  "fm.name": "名称",
  "fm.lastModified": "修改时间",
  "fm.size": "大小",
  "fm.actions": "操作",
  "drawer.loadFailed": "读取文件失败",
  "drawer.reloaded": "已重新加载",
  "drawer.unsavedChanges": "文件已修改，是否保存？",
  "drawer.saved": "✓ 已保存",
  "drawer.saveFailed": "保存失败",
  "drawer.unsupportedPreview": "不支持预览此文件类型",
  "drawer.loading": "加载中...",
  "drawer.retry": "重试",
  "drawer.preview": "预览",
  "drawer.edit": "编辑",
  "drawer.externallyModified": "已被外部修改",
  "drawer.fileDeletedExternally": "文件已被外部删除",
  "drawer.close": "关闭",
  "drawer.saving": "保存中...",
  "drawer.modified": "● 已修改",
  "drawer.reload": "重新加载",
  "preview.loadFailed": "图片加载失败",
  "preview.download": "下载文件",
  "mobile.terminal": "终端",
  "mobile.files": "文件",
  "mobile.sessions": "会话",
  "mobile.settings": "设置",
  "modal.confirm": "确认",
  "modal.cancel": "取消",
  "modal.processing": "处理中..."
}
```

- [ ] **Step 2: 创建英文翻译文件**

Create `frontend/src/locales/en/translation.json`:

```json
{
  "sidebar.expand": "Expand sidebar",
  "sidebar.collapse": "Collapse sidebar",
  "sidebar.workspaces": "Workspaces",
  "sidebar.sessions": "Sessions",
  "sidebar.createWorkspace": "New Workspace",
  "sidebar.workspaceName": "Name",
  "sidebar.rootPath": "Root Path",
  "sidebar.cancel": "Cancel",
  "sidebar.create": "Create",
  "sidebar.creating": "Creating...",
  "sidebar.createSession": "New Session",
  "sidebar.sessionName": "Session Name",
  "sidebar.optional": "(optional)",
  "sidebar.deleteWorkspace": "Delete Workspace",
  "sidebar.deleteSession": "Delete Session",
  "sidebar.confirmDeleteWorkspace": "Are you sure you want to delete workspace \"{{name}}\"? This cannot be undone.",
  "sidebar.confirmDeleteSession": "Are you sure you want to delete session \"{{name}}\"? The corresponding tmux session will also be terminated.",
  "sidebar.delete": "Delete",
  "sidebar.workspaceCreated": "Workspace \"{{name}}\" created",
  "sidebar.sessionCreated": "Session \"{{name}}\" created",
  "sidebar.workspaceDeleted": "Workspace \"{{name}}\" deleted",
  "sidebar.sessionDeleted": "Session \"{{name}}\" deleted",
  "sidebar.unnamed": "Unnamed",
  "sidebar.connected": "Connected",
  "sidebar.disconnected": "Disconnected",
  "sidebar.noWorkspaces": "No workspaces. Click + to create one.",
  "sidebar.noSessions": "No sessions",
  "settings.title": "Settings",
  "settings.theme": "Theme",
  "settings.light": "Light",
  "settings.dark": "Dark",
  "settings.system": "System",
  "settings.fontSize": "Terminal Font Size",
  "settings.language": "Language",
  "settings.about": "About",
  "terminal.noSession": "Select or create a session",
  "terminal.hint": "Use the sidebar to get started",
  "terminal.status.connected": "Connected",
  "terminal.status.disconnected": "Disconnected",
  "terminal.status.connectionError": "Connection error",
  "terminal.status.attached": "Attached to {{session}}",
  "terminal.status.error": "Error: {{msg}}",
  "terminal.status.exited": "Process exited: {{code}}",
  "fm.loadFailed": "Failed to load file list",
  "fm.uploadComplete": "Upload complete",
  "fm.uploadFailed": "Upload failed",
  "fm.uploadFileFailed": "Failed to upload {{name}}: {{msg}}",
  "fm.renameSuccess": "Renamed successfully",
  "fm.renameFailed": "Rename failed",
  "fm.confirmDelete": "Delete {{count}} item(s)?",
  "fm.deleted": "Deleted {{count}} item(s)",
  "fm.deleteFailed": "Delete failed",
  "fm.searchFailed": "Search failed",
  "fm.selectSessionFirst": "Please select a terminal session from the sidebar",
  "fm.loading": "Loading...",
  "fm.emptyDir": "This directory is empty",
  "fm.dragHint": "Drag and drop files here to upload",
  "fm.searchPlaceholder": "Search file names...",
  "fm.backToParent": "Go to parent directory",
  "fm.search": "Search files",
  "fm.refresh": "Refresh",
  "fm.upload": "Upload file",
  "fm.collapse": "Collapse file manager",
  "fm.expand": "Expand file manager",
  "fm.rename": "Rename",
  "fm.delete": "Delete",
  "fm.items": "items",
  "fm.backToTerminalDir": "Go to terminal working directory",
  "fm.outOfWorkspace": "Current directory is outside workspace boundary",
  "fm.name": "Name",
  "fm.lastModified": "Last Modified",
  "fm.size": "Size",
  "fm.actions": "Actions",
  "drawer.loadFailed": "Failed to read file",
  "drawer.reloaded": "Reloaded",
  "drawer.unsavedChanges": "File has been modified. Save changes?",
  "drawer.saved": "✓ Saved",
  "drawer.saveFailed": "Save failed",
  "drawer.unsupportedPreview": "Preview not supported for this file type",
  "drawer.loading": "Loading...",
  "drawer.retry": "Retry",
  "drawer.preview": "Preview",
  "drawer.edit": "Edit",
  "drawer.externallyModified": "Modified externally",
  "drawer.fileDeletedExternally": "File has been deleted externally",
  "drawer.close": "Close",
  "drawer.saving": "Saving...",
  "drawer.modified": "● Modified",
  "drawer.reload": "Reload",
  "preview.loadFailed": "Failed to load image",
  "preview.download": "Download file",
  "mobile.terminal": "Terminal",
  "mobile.files": "Files",
  "mobile.sessions": "Sessions",
  "mobile.settings": "Settings",
  "modal.confirm": "Confirm",
  "modal.cancel": "Cancel",
  "modal.processing": "Processing..."
}
```

- [ ] **Step 3: 创建 i18n 初始化模块**

Create `frontend/src/i18n.ts`:

```ts
import i18n from 'i18next'
import { initReactI18next } from 'react-i18next'
import LanguageDetector from 'i18next-browser-languagedetector'
import zh from './locales/zh/translation.json'
import en from './locales/en/translation.json'

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      zh: { translation: zh },
      en: { translation: en },
    },
    fallbackLng: 'en',
    detection: {
      order: ['localStorage', 'navigator'],
      lookupLocalStorage: 'omniterm_locale',
      caches: ['localStorage'],
    },
    interpolation: {
      escapeValue: false,
    },
  })

export default i18n
```

- [ ] **Step 4: 在 main.tsx 中导入 i18n**

Edit `frontend/src/main.tsx`，在 `import './index.css'` 之后添加 `import './i18n'`：

```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import './i18n'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

- [ ] **Step 5: 验证编译**

```bash
cd /home/pax/coding/OmniTerm-dev/frontend && pnpm build
```

- [ ] **Step 6: 提交**

```bash
cd /home/pax/coding/OmniTerm-dev
git add frontend/src/i18n.ts frontend/src/locales/ frontend/src/main.tsx
git commit -m "feat(i18n): 添加 i18next 配置和中英翻译文件"
```

---

### Task 3: Settings 语言选择器

**Files:**
- Modify: `frontend/src/components/Settings/Settings.tsx`

- [ ] **Step 1: 替换 Settings.tsx**

用以下完整内容替换 `frontend/src/components/Settings/Settings.tsx`：

```tsx
import { useTranslation } from 'react-i18next'
import { useThemeStore, type Theme } from '../../stores/themeStore'
import { useAppStore } from '../../stores/appStore'

const themes: { value: Theme; labelKey: string; icon: string }[] = [
  { value: 'light', labelKey: 'settings.light', icon: '☀️' },
  { value: 'dark', labelKey: 'settings.dark', icon: '🌙' },
  { value: 'system', labelKey: 'settings.system', icon: '💻' },
]

const languages = [
  { value: 'zh', label: '中文' },
  { value: 'en', label: 'English' },
]

export function Settings() {
  const { t, i18n } = useTranslation()
  const { theme, setTheme } = useThemeStore()
  const { fontSize, setFontSize } = useAppStore()

  return (
    <div className="h-full overflow-y-auto bg-gray-50 dark:bg-gray-900 text-gray-900 dark:text-gray-100">
      <div className="max-w-lg mx-auto p-4 space-y-6">
        <h2 className="text-lg font-semibold">{t('settings.title')}</h2>

        {/* Theme */}
        <section className="space-y-3">
          <h3 className="text-sm font-medium text-gray-500 dark:text-gray-400">{t('settings.theme')}</h3>
          <div className="flex gap-2">
            {themes.map((th) => (
              <button
                key={th.value}
                onClick={() => setTheme(th.value)}
                className={`flex-1 flex items-center justify-center gap-2 px-3 py-2.5 rounded-lg text-sm transition-colors ${
                  theme === th.value
                    ? 'bg-blue-500 text-white shadow-sm'
                    : 'bg-white dark:bg-gray-800 text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 border border-gray-200 dark:border-gray-700'
                }`}
              >
                <span>{th.icon}</span>
                <span>{t(th.labelKey)}</span>
              </button>
            ))}
          </div>
        </section>

        {/* Language */}
        <section className="space-y-3">
          <h3 className="text-sm font-medium text-gray-500 dark:text-gray-400">{t('settings.language')}</h3>
          <div className="flex gap-2">
            {languages.map((lang) => (
              <button
                key={lang.value}
                onClick={() => i18n.changeLanguage(lang.value)}
                className={`flex-1 flex items-center justify-center px-3 py-2.5 rounded-lg text-sm transition-colors ${
                  i18n.language === lang.value || i18n.language.startsWith(lang.value)
                    ? 'bg-blue-500 text-white shadow-sm'
                    : 'bg-white dark:bg-gray-800 text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 border border-gray-200 dark:border-gray-700'
                }`}
              >
                <span>{lang.label}</span>
              </button>
            ))}
          </div>
        </section>

        {/* Font size */}
        <section className="space-y-3">
          <h3 className="text-sm font-medium text-gray-500 dark:text-gray-400">
            {t('settings.fontSize')}
          </h3>
          <div className="flex items-center gap-3">
            <button
              onClick={() => setFontSize(fontSize - 1)}
              disabled={fontSize <= 10}
              className="w-9 h-9 flex items-center justify-center rounded-lg bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 hover:bg-gray-100 dark:hover:bg-gray-700 disabled:opacity-40 transition-colors text-lg"
            >
              −
            </button>
            <div className="flex-1 text-center">
              <span className="text-2xl font-mono font-semibold">{fontSize}</span>
              <span className="text-xs text-gray-400 ml-1">px</span>
            </div>
            <button
              onClick={() => setFontSize(fontSize + 1)}
              disabled={fontSize >= 24}
              className="w-9 h-9 flex items-center justify-center rounded-lg bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 hover:bg-gray-100 dark:hover:bg-gray-700 disabled:opacity-40 transition-colors text-lg"
            >
              +
            </button>
          </div>
          <input
            type="range"
            min={10}
            max={24}
            value={fontSize}
            onChange={(e) => setFontSize(Number(e.target.value))}
            className="w-full accent-blue-500"
          />
        </section>

        {/* Info */}
        <section className="space-y-3">
          <h3 className="text-sm font-medium text-gray-500 dark:text-gray-400">{t('settings.about')}</h3>
          <div className="text-sm text-gray-600 dark:text-gray-400 space-y-1">
            <p>OmniTerm — Web-based tmux terminal manager</p>
            <p className="text-xs">Phase 7 · MIT License</p>
          </div>
        </section>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 验证编译**

```bash
cd /home/pax/coding/OmniTerm-dev/frontend && pnpm build
```

- [ ] **Step 3: 提交**

```bash
cd /home/pax/coding/OmniTerm-dev
git add frontend/src/components/Settings/Settings.tsx
git commit -m "feat(i18n): Settings 添加语言选择器，所有设置文案国际化"
```

---

### Task 4: Sidebar 国际化

**Files:**
- Modify: `frontend/src/components/Sidebar/Sidebar.tsx`

- [ ] **Step 1: 添加 import 和 hook**

在 `Sidebar.tsx` 顶部 import 区添加：

```tsx
import { useTranslation } from 'react-i18next'
```

在 Sidebar 函数体中（`useAppStore` 之后）添加：

```tsx
const { t } = useTranslation()
```

- [ ] **Step 2: 替换所有硬编码字符串**

按以下映射逐一替换（全部在同一文件内）：

| 行号附近 | 原文 | 替换为 |
|---------|------|--------|
| L184 | `title="展开侧边栏"` | `title={t('sidebar.expand')}` |
| L201 | `title="Settings"` (collapsed) | `title={t('settings.title')}` |
| L245 | `title="收起侧边栏"` | `title={t('sidebar.collapse')}` |
| L258 | `Workspaces` | `{t('sidebar.workspaces')}` |
| L267 | `title="New Workspace"` | `title={t('sidebar.createWorkspace')}` |
| L283 | `No workspaces. Click + to create one.` | `{t('sidebar.noWorkspaces')}` |
| L342 | `Sessions` | `{t('sidebar.sessions')}` |
| L347 | `title="New Session"` | `title={t('sidebar.createSession')}` |
| L399 | `'未命名'` | `t('sidebar.unnamed')` |
| L409 | `No sessions` | `{t('sidebar.noSessions')}` |
| L435 | `connected ? 'Connected' : 'Disconnected'` | `connected ? t('sidebar.connected') : t('sidebar.disconnected')` |
| L441 | `title="Settings"` (bottom gear) | `title={t('settings.title')}` |
| L458 | `title="创建工作区"` | `title={t('sidebar.createWorkspace')}` |
| L462 | `名称` | `{t('sidebar.workspaceName')}` |
| L479 | `根路径` | `{t('sidebar.rootPath')}` |
| L494 | `取消` (ModalCancel children) | `{t('sidebar.cancel')}` |
| L498 | `submitting ? '创建中...' : '创建'` | `submitting ? t('sidebar.creating') : t('sidebar.create')` |
| L505 | `title="创建会话"` | `title={t('sidebar.createSession')}` |
| L509 | `会话名称` | `{t('sidebar.sessionName')}` |
| L509 | `(可选)` | `{t('sidebar.optional')}` |
| L525 | `取消` (ModalCancel children) | `{t('sidebar.cancel')}` |
| L529 | `submitting ? '创建中...' : '创建'` | `submitting ? t('sidebar.creating') : t('sidebar.create')` |

- [ ] **Step 3: 替换 toast 消息**

```tsx
// L89: 工作区创建成功
addToast('success', t('sidebar.workspaceCreated', { name: wsName.trim() }))

// L107: 会话创建成功
addToast('success', t('sidebar.sessionCreated', { name: sessName.trim() || t('sidebar.unnamed') }))

// L126: 工作区已删除
addToast('success', t('sidebar.workspaceDeleted', { name: confirmDelete.name }))

// L144: 会话已删除
addToast('success', t('sidebar.sessionDeleted', { name: confirmDelete.name }))
```

- [ ] **Step 4: 替换 ConfirmDialog props**

```tsx
<ConfirmDialog
  open={!!confirmDelete}
  onClose={() => setConfirmDelete(null)}
  onConfirm={confirmDelete?.type === 'workspace' ? handleDeleteWorkspace : handleDeleteSession}
  title={confirmDelete?.type === 'workspace' ? t('sidebar.deleteWorkspace') : t('sidebar.deleteSession')}
  message={
    confirmDelete?.type === 'workspace'
      ? t('sidebar.confirmDeleteWorkspace', { name: confirmDelete?.name })
      : t('sidebar.confirmDeleteSession', { name: confirmDelete?.name })
  }
  confirmText={t('sidebar.delete')}
  destructive
  loading={submitting}
/>
```

- [ ] **Step 5: 替换 DeleteButton title**

```tsx
title={t('sidebar.delete')}
```

- [ ] **Step 6: 验证编译**

```bash
cd /home/pax/coding/OmniTerm-dev/frontend && pnpm build
```

- [ ] **Step 7: 提交**

```bash
cd /home/pax/coding/OmniTerm-dev
git add frontend/src/components/Sidebar/Sidebar.tsx
git commit -m "feat(i18n): Sidebar 国际化"
```

---

### Task 5: FileManager 国际化

**Files:**
- Modify: `frontend/src/components/FileManager/FileManager.tsx`

- [ ] **Step 1: 添加 import 和 hook**

```tsx
import { useTranslation } from 'react-i18next'
```

在 FileManager 函数体中（`useAppStore` 之后）添加：

```tsx
const { t } = useTranslation()
```

- [ ] **Step 2: 替换 fetchFiles 中的错误消息**

```tsx
// L131
addToast('error', err.message || t('fm.loadFailed'))
```

- [ ] **Step 3: 替换 handleDrop 中的消息**

```tsx
// L298
addToast('error', t('fm.uploadFileFailed', { name: file.name, msg: err.message }))

// L301
addToast('success', t('fm.uploadComplete'))
```

- [ ] **Step 4: 替换 commitRename 中的消息**

```tsx
// L317
addToast('success', t('fm.renameSuccess'))

// L320
addToast('error', err.message || t('fm.renameFailed'))
```

- [ ] **Step 5: 替换 handleDelete 中的消息**

```tsx
// L327
if (!confirm(t('fm.confirmDelete', { count: selected.size }))) return

// L332
addToast('success', t('fm.deleted', { count: selected.size }))

// L335
addToast('error', err.message || t('fm.deleteFailed'))
```

- [ ] **Step 6: 替换 handleUpload 中的消息**

```tsx
// L350
addToast('error', t('fm.uploadFileFailed', { name: input.files[i].name, msg: err.message }))

// L353
addToast('success', t('fm.uploadComplete'))
```

- [ ] **Step 7: 替换 handleSearch 中的错误消息**

```tsx
// L366
addToast('error', err.message || t('fm.searchFailed'))
```

- [ ] **Step 8: 替换 collapsed state 的 title**

```tsx
// L389
title={t('fm.expand')}

// L405
title={t('fm.expand')}
```

- [ ] **Step 9: 替换 toolbar 按钮 title**

```tsx
// L427
title={t('fm.collapse')}

// L437
title={t('fm.search')}

// L442
placeholder={t('fm.searchPlaceholder')}

// L463
title={t('fm.backToParent')}

// L466
title={t('fm.refresh')}

// L469
title={t('fm.upload')}
```

- [ ] **Step 10: 替换 breadcrumb title**

```tsx
// L481
title={t('fm.backToTerminalDir')}

// L495
title={t('fm.outOfWorkspace')}
```

- [ ] **Step 11: 替换空状态和加载状态文本**

```tsx
// L512
<span>{t('fm.selectSessionFirst')}</span>

// L515
<div className="fm-empty">{t('fm.loading')}</div>

// L519
<span>{t('fm.emptyDir')}</span>

// L520
<span className="fm-empty-hint">{t('fm.dragHint')}</span>
```

- [ ] **Step 12: 替换表头**

```tsx
// L535: Name
{t('fm.name')} <SI col="name" />

// L541: Last Modified
{t('fm.lastModified')} <SI col="mtime" />

// L547: Size
{t('fm.size')} <SI col="size" />

// L551: Actions
{t('fm.actions')}
```

- [ ] **Step 13: 替换操作按钮 title 和目录项文本**

```tsx
// L591: 目录项数
{isDir ? `${f.size} ${t('fm.items')}` : formatSize(f.size)}

// L595: 重命名 title
title={t('fm.rename')}

// L602: 删除 title
title={t('fm.delete')}
```

- [ ] **Step 14: 验证编译并提交**

```bash
cd /home/pax/coding/OmniTerm-dev/frontend && pnpm build
cd /home/pax/coding/OmniTerm-dev
git add frontend/src/components/FileManager/FileManager.tsx
git commit -m "feat(i18n): FileManager 国际化"
```

---

### Task 6: FileDrawer + FilePreview 国际化

**Files:**
- Modify: `frontend/src/components/FileManager/FileDrawer.tsx`
- Modify: `frontend/src/components/FileManager/FilePreview.tsx`

- [ ] **Step 1: FileDrawer 添加 import 和 hook**

```tsx
import { useTranslation } from 'react-i18next'
```

在 FileDrawer 函数体中添加：

```tsx
const { t } = useTranslation()
```

- [ ] **Step 2: 替换 FileDrawer 中的硬编码字符串**

| 原文 | 替换为 |
|------|--------|
| `'读取文件失败'` | `t('drawer.loadFailed')` |
| `'已重新加载'` | `t('drawer.reloaded')` |
| `'文件已修改，是否保存？'` | `t('drawer.unsavedChanges')` |
| `'✓ 已保存'` | `t('drawer.saved')` |
| `'保存失败'` | `t('drawer.saveFailed')` |
| `'文件已被外部删除'` | `t('drawer.fileDeletedExternally')` |
| `已被外部修改` | `{t('drawer.externallyModified')}` |
| `不支持预览此文件类型` | `{t('drawer.unsupportedPreview')}` |
| `加载中...` | `{t('drawer.loading')}` |
| `重试` | `{t('drawer.retry')}` |
| `预览` | `{t('drawer.preview')}` |
| `编辑` | `{t('drawer.edit')}` |
| `title="关闭"` | `title={t('drawer.close')}` |
| `重新加载` | `{t('drawer.reload')}` |
| `保存中...` | `{t('drawer.saving')}` |
| `● 已修改` | `{t('drawer.modified')}` |

- [ ] **Step 3: FilePreview 添加 import 和 hook**

```tsx
import { useTranslation } from 'react-i18next'
```

在 FilePreview 函数体中添加：

```tsx
const { t } = useTranslation()
```

- [ ] **Step 4: 替换 FilePreview 中的硬编码字符串**

```tsx
// 图片加载失败
<span>{t('preview.loadFailed')}</span>

// 下载文件
{t('preview.download')}
```

- [ ] **Step 5: 验证编译并提交**

```bash
cd /home/pax/coding/OmniTerm-dev/frontend && pnpm build
cd /home/pax/coding/OmniTerm-dev
git add frontend/src/components/FileManager/FileDrawer.tsx frontend/src/components/FileManager/FilePreview.tsx
git commit -m "feat(i18n): FileDrawer + FilePreview 国际化"
```

---

### Task 7: Terminal + MobileNav + ConfirmDialog 国际化

**Files:**
- Modify: `frontend/src/components/Terminal/Terminal.tsx`
- Modify: `frontend/src/components/Layout/MobileNav.tsx`
- Modify: `frontend/src/components/Modal/ConfirmDialog.tsx`

- [ ] **Step 1: Terminal.tsx 添加 import 和 hook**

```tsx
import { useTranslation } from 'react-i18next'
```

在 Terminal 函数体中添加：

```tsx
const { t } = useTranslation()
```

替换空状态文本：

```tsx
// L36
<div style={{ fontSize: 14, color: '#94a3b8' }}>{t('terminal.noSession')}</div>

// L38
<div style={{ fontSize: 12, marginTop: 8, color: '#475569' }}>
  {t('terminal.hint')}
</div>
```

- [ ] **Step 2: MobileNav.tsx 添加 import 和 hook**

```tsx
import { useTranslation } from 'react-i18next'
```

在 MobileNav 函数体中添加：

```tsx
const { t } = useTranslation()
```

替换 tabs 数组为使用翻译 key：

```tsx
const tabs = [
  { id: 'terminal' as const, labelKey: 'mobile.terminal', icon: '⌨️' },
  { id: 'files' as const, labelKey: 'mobile.files', icon: '📁' },
  { id: 'sessions' as const, labelKey: 'mobile.sessions', icon: '📋' },
  { id: 'settings' as const, labelKey: 'mobile.settings', icon: '⚙️' },
]
```

在渲染处将 `tab.label` 改为 `t(tab.labelKey)`：

```tsx
<span className="block mt-1">{t(tab.labelKey)}</span>
```

- [ ] **Step 3: ConfirmDialog.tsx 添加 import 和默认值翻译**

```tsx
import { useTranslation } from 'react-i18next'
```

在 ConfirmDialog 函数体中添加：

```tsx
const { t } = useTranslation()
```

替换默认值和硬编码文本：

```tsx
// confirmText 默认值从 '确认' 改为使用 t()
// 注意：不能在参数默认值中使用 hook，需要在函数体内处理
confirmText, // 移除默认值，在函数体内用 confirmText ?? t('modal.confirm')

// 取消按钮
{t('modal.cancel')}

// 处理中...
{loading ? t('modal.processing') : (confirmText ?? t('modal.confirm'))}
```

具体实现：将 confirmText 的默认值逻辑移到函数体内：

```tsx
export function ConfirmDialog({
  open,
  onClose,
  onConfirm,
  title,
  message,
  confirmText,
  destructive = false,
  loading = false,
}: ConfirmDialogProps) {
  const { t } = useTranslation()
  const resolvedConfirmText = confirmText ?? t('modal.confirm')
  // ... 使用 resolvedConfirmText 替代 confirmText
}
```

- [ ] **Step 4: 验证编译并提交**

```bash
cd /home/pax/coding/OmniTerm-dev/frontend && pnpm build
cd /home/pax/coding/OmniTerm-dev
git add frontend/src/components/Terminal/Terminal.tsx frontend/src/components/Layout/MobileNav.tsx frontend/src/components/Modal/ConfirmDialog.tsx
git commit -m "feat(i18n): Terminal + MobileNav + ConfirmDialog 国际化"
```

---

### Task 8: useTerminal 国际化

**Files:**
- Modify: `frontend/src/hooks/useTerminal.ts`

- [ ] **Step 1: 添加 import**

```tsx
import { useTranslation } from 'react-i18next'
```

在 useTerminal 函数体中添加：

```tsx
const { t } = useTranslation()
```

- [ ] **Step 2: 替换终端状态消息**

注意：`t()` 在 `useCallback` 内部使用时需要确保 `t` 在依赖数组中，或者使用 `i18n.t()` 避免闭包问题。推荐使用 `i18n.t()` 方式：

```tsx
import { useTranslation } from 'react-i18next'

// 在函数体中
const { i18n } = useTranslation()

// L48: connected
termRef.current?.writeln(`\x1b[32m[${i18n.t('terminal.status.connected')}]\x1b[0m`)

// L58: attached
termRef.current?.writeln(`\x1b[36m[${i18n.t('terminal.status.attached', { session: msg.session })}]\x1b[0m`)

// L60: error
termRef.current?.writeln(`\x1b[31m[${i18n.t('terminal.status.error', { msg: msg.message })}]\x1b[0m`)

// L62: exit
termRef.current?.writeln(`\x1b[31m[${i18n.t('terminal.status.exited', { code: msg.code })}]\x1b[0m`)

// L72: disconnected
termRef.current?.writeln(`\x1b[31m[${i18n.t('terminal.status.disconnected')}]\x1b[0m`)

// L79: connection error
termRef.current?.writeln(`\x1b[31m[${i18n.t('terminal.status.connectionError')}]\x1b[0m`)
```

- [ ] **Step 3: 验证编译并提交**

```bash
cd /home/pax/coding/OmniTerm-dev/frontend && pnpm build
cd /home/pax/coding/OmniTerm-dev
git add frontend/src/hooks/useTerminal.ts
git commit -m "feat(i18n): useTerminal 终端状态消息国际化"
```

---

### Task 9: 最终验证

- [ ] **Step 1: 完整构建验证**

```bash
cd /home/pax/coding/OmniTerm-dev/frontend && pnpm build
```

Expected: 编译成功，无报错。

- [ ] **Step 2: 手动测试清单**

启动 dev server (`./dev.sh start`)，在浏览器中验证：

1. **语言自动检测**：清除 localStorage 中的 `omniterm_locale`，刷新页面，确认根据浏览器语言自动选择
2. **语言手动切换**：在设置中切换语言，确认所有 UI 文本即时刷新
3. **语言持久化**：切换语言后刷新页面，确认语言偏好保持
4. **Sidebar**：展开/收起、创建/删除工作区和会话的模态框、toast 消息
5. **FileManager**：表头、空状态、搜索、上传、删除确认
6. **FileDrawer**：预览/编辑按钮、保存/重载消息、状态栏
7. **Terminal**：空状态提示、连接/断开状态消息
8. **MobileNav**：底部标签文案（移动端视图）
9. **Settings**：语言选择器、主题切换

- [ ] **Step 3: 提交 CHANGELOG**

在 `CHANGELOG.md` 中添加条目（如果项目有的话）。
