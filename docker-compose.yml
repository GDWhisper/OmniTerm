services:
  omniterm:
    build: .
    ports:
      - "9077:9077"
    volumes:
      - omniterm-data:/app/data
      - /home:/home:ro
    environment:
      - DATABASE_URL=sqlite:/app/data/omniterm.db?mode=rwc
      - JWT_SECRET=${JWT_SECRET:-change-me-in-production}
      - BIND_ADDR=0.0.0.0:9077
    restart: unless-stopped

volumes:
  omniterm-data:
